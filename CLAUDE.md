# CLAUDE.md — Manifest Real Estate AI Companion

Guidance for Claude Code working in this repo.
- **Current focus → the overhaul.** Plan: `~/.claude/plans/we-need-to-to-sequential-deer.md`. Living status: `Fresh_Build/PROJECT_TRACKER.md`. New build lives in `Fresh_Build/`.
- **Legacy demo** (below) stays as reference: `legacy/docs/plan.md`, `legacy/docs/PROJECT_TRACKER.md`, `legacy/demo_theme/`, `legacy/web/`, `api/`.

## Current focus — the overhaul (`Fresh_Build/`)
Full overhaul of the Manifest site for first-home-buyers / early investors / 1–2-property owners — give them broker-grade tools and build trust → leads. Three milestones:
1. **Live website + Vault RE listings** (create a listing in Vault RE → it appears on the site, via Vault RE's REST API).
2. **Client tools:** Instant Valuation, Portfolio assessment, Pre-buying tool, VIC Stamp-duty calculator, Rental yield/cash-flow tool (deterministic, from sro.vic.gov.au).
3. **Trust engine:** Melbourne newsletter (adapt the owner's existing email-approval content-bot pattern) + monthly per-client market updates. **NOT part of the website** — it's a separate engine.
Then a companion **app** reusing the same tools. Foundation: **custom React** (like `legacy/demo_theme`), built local-first / host-agnostic. Hard cap **≤ $500/mo**.

**Build progress (2026-06-30):** The new site lives in `Fresh_Build/site/` (Vite+React+TS+Tailwind, dark theme on :5180, proxies `/api`→:8787). Built: Home (motion hero, neutral near-black — blue tint removed; headline **"Make your next move / with Conviction"** — "Conviction" is a gold-gradient word that settles + shimmers; the skyline SVG now has ~548 warm **lit windows**), Listings (mock), About, Contact (lead capture), and **all 6 client tools** — Instant Valuation, AI Concierge (floating bot + page), Stamp Duty, **Borrowing Capacity Calculator** (renamed from Pre-buying), Portfolio, Rental. Deterministic calculators verified against sources; **all sliders are now text boxes**. Tools live in a 6-bucket mega-menu; plus a **"First home buyers" nav dropdown** → a 7-step buyer-journey page (`/first-home-buyers/steps`) and a curated buyer-tools page (`/first-home-buyers/tools`). Borrowing Capacity has an **"Email me my report" lead-capture CTA**; a brand-styled **sample report** (HTML + PDF) lives in `Fresh_Build/Assets/`. **Akshay is branded "AK"** in casual CTAs (full name "Akshay Kapoor (AK)" on About; review quotes verbatim); the Borrowing Capacity CTA is "Speak to a RE representative". **Every route opens at the top** (`Shell.tsx` scroll-to-top on path change); the **mobile menu's** Listings/About/Contact are gold→white-on-hover. Logo = original badge recolored to **brand navy `#003970`** (gold accents kept), 116px, in a 128px header. Reuses the legacy `api/` (Hono) for valuation/chat/leads — the **valuation comp price/property mismatch is fixed** (`api/src/valuation.ts` `reconcileComps`). **Preview:** GitHub Pages at https://samsteve159.github.io/Real_Estate_web/ — **now serves V2** (repointed 2026-07-02; V1 no longer deployed). Static, so **no `/api` there**: valuation/concierge/lead+report forms no-op and listings fall back to mocks; deterministic tools + steps page work. **Pages gotcha (hit 2026-07-28):** making the repo **private disables Pages on the free plan** (preview 404s), and flipping back to public does **not** re-enable it — someone must re-set **Settings → Pages → Source: GitHub Actions** and push. If the preview must stay private, move it to Cloudflare Pages/Netlify (free, build from private repos) rather than toggling visibility. Live AI needs the `ANTHROPIC_API_KEY` in the **repo-root `.env`** (loaded by `api/src/anthropic.ts` via dotenv — not `api/.env`).

**V2 — agency repositioning (2026-07-02):** Per Akshay's feedback (`Fresh_Build/Assets/akshay-feedback-2026-07-02.md`), a repositioned build lives in **`Fresh_Build/site-v2/`** (moved there 2026-07-02; dev on **:5181**; V1 stays in `Fresh_Build/site/` on :5180 for backtracking). Reframes Manifest as a **boutique full-service agency + advisory** with **evergreen** copy (no live-market language). Nav → Home · Listings · **Services** mega-menu · About · Contact. Five evergreen service pages (Residential Sales, Acreage & Lifestyle, Development Projects, Commercial Leasing, Property Advisory) via a shared `ServicePage.tsx`; Home hero swapped to "Property. Opportunity. Results." (motion kept). The 6 tools + first-home-buyer journey are **kept, relocated under Property Advisory**. Brand kept **"Manifest Real Estate"**. **Feedback rounds 2–3 applied + deployed (2026-07-02):** service-page copy edits (Dev Projects adds "08 Coordinating Finance / 09 Exit Strategy" + "residential and commercial sites"; Residential "First Home Buyer Sales"; Commercial "Blue Chip Tenant Sourcing"; Property Advisory trimmed to 6 areas); a **hidden `RecentProjects.tsx`** showcase on Dev Projects + Commercial Leasing (`SHOW=false`); About reworked — enlarged **"Meet the Directors"**, photos moved below names at equal width, **Rishi headshot re-cropped** (800×800) + tone-matched, both **real Rishi reviews** added (Home `TrustBand` now shows per-director review blocks); Contact split into **separate Phone/Email** + address removed + Servicing "Victoria"; **inner-page top padding 6→9rem** (was hiding under the 128px header) and **mobile menu** capped to viewport so About/Contact stay reachable. **V2 is LOCKED (2026-07-03)** — Akshay signed off on the deployed V2 as the chosen build; V1 (`Fresh_Build/site/`) is archived reference only, no more backtracking. Latest local change: the Instant Valuation "Reading the comparable sales…" text swapped for an on-brand **gold spinner** (`site-v2/src/routes/ValuationPage.tsx`). **Still open for Akshay:** brand name ("Manifest Deal"?) and an **Insights** page (named, no copy yet).

**Vault RE integration LIVE (2026-07-25):** MRI provisioned the integrator **API key** (`VAULT_API_KEY`) and Akshay generated the per-office **Access Token** (`VAULT_ACCESS_TOKEN`) — both in repo-root `.env` (Integration "Manifest Real Estate", Office "Manifest Real Estate (5478)"). `api/src/vault.ts` (VaultRE client: two-header auth `X-Api-Key` + `Authorization: Bearer`, `GET /properties/residential/sale`, pagination, normaliser → site listing shape, 10-min cache, graceful fallback) + endpoint `GET /api/vault/listings` (`{isLive, listings}`) + front-end wiring (`site-v2` `getVaultListings()`; `ListingsPage`/`ListingsPreview` swap placeholders → live data, banner hidden when live, fall back to mock when API unreachable). **Verified: 14 real Manifest for-sale listings flow end-to-end**, normalised cleanly (state from `address.suburb.state.abbreviation`; suburb title-cased; POA prices cleaned; published photos). Details: `Fresh_Build/PROJECT_TRACKER.md` (V2 section).

**Listing detail page · self-hosted photos · enquiry emails (2026-07-28, commit `c9c5a71`):** the three Vault follow-ups are **done**. (1) **Listing detail page** at **`/listings/:id`** (not `/properties/{id}` as earlier docs guessed) — `routes/ListingDetailPage.tsx` (gallery + thumbnails, meta, description, land size, agent card, enquiry form), backed by `getVaultListingDetail()` + `GET /api/vault/listings/:id`; grid cards on `ListingsPage`/`ListingsPreview` now link there (before this, clicking a listing did nothing). Verified against **all 14 live listings**; two field-name guesses were wrong and are fixed against a real response — it's **`landArea.units`** (not `.unit`) and **`contactStaff[]`** (not `listers[]`, phones under `phoneNumbers[{number,typeCode:"M"}]`). (2) **Self-hosted photos** — `api/src/vaultPhoto.ts`; every photo URL is rewritten to `/api/vault/photo?src=…`, downloaded from Vault once and served from `data/vault-photos/` (gitignored) after, satisfying Vault's no-hotlinking policy. **Host-allowlisted to `*.vaultre.com.au` — keep that guard**, it's what stops the endpoint being an open image-fetch proxy (SSRF). (3) **Enquiry emails** — `api/src/email.ts` (Resend), wired fire-and-forget into `POST /api/lead`: admin notification + auto-reply, and the Borrowing Capacity "email me my report" CTA now sends the real summary. **No-ops gracefully when `RESEND_API_KEY` is unset** (same pattern as `vault.ts`) — so until Resend is set up, **enquiries save to SQLite and nobody is emailed**. `.env.example` was stale (missing every Vault var) and is now current.

**Borrowing Capacity report is a real PDF (2026-08-03, `556d6d8`).** `api/src/report.ts` renders a branded A4 PDF via **PDFKit** — deliberately *not* headless Chrome, since Puppeteer would cost ~300 MB resident per render on the 1 vCPU / 2 GB box. Navy letterhead, gold rule, headline borrowing figure, serviceability banner, figures + inputs tables, indicative-only disclaimer; attached to the report email, falling back to sending without the attachment if rendering fails. `POST /api/lead` takes a structured `report` object clamped through the same guards as every other input. **Verified 2026-08-03** — both variants render as valid PDFs. (Any earlier note saying "HTML only, the toggle is cosmetic" is superseded.)

**Resend needs a domain you own — a hard blocker on all enquiry email (found 2026-08-04).** Their docs: *"Resend sends emails using a domain you own (not a shared or public domain). You must add and verify at least one domain."* So the `onboarding@resend.dev` default in `email.ts` **cannot work**, and that code comment ("works with no DNS setup") is wrong and **still needs fixing**. Plan: add **`send.manifestre.com.au`** as a subdomain in Resend so SPF/DKIM/MX hang off the subdomain and **touch nothing the live WordPress site uses** — still needs **Akshay's explicit OK** even though we hold GoDaddy DNS access. GoDaddy gotcha: it auto-appends the domain, so the host field is `send`, not the full name. Free tier: $0/mo, **3,000 emails/month, 100/day, 1 domain**. **Who gets what:** every lead sends an admin notification to the single `LEADS_NOTIFY_EMAIL` (with `Reply-To` set to the enquirer) plus an auto-reply to the enquirer — contact form, listing enquiry, valuation, concierge capture and the report CTA **all land in that one inbox**; only `intent: "report"` gets the PDF attached.

**Deployed to the GoDaddy VPS (2026-08-03) — NOT LIVE.** `manifest-prod`, **97.74.94.97**, Ubuntu 24.04, 1 vCPU / 1.9 GB / 38 GB, Singapore, **monthly** term (no 3-yr lock). Key-based SSH (`~/.ssh/manifest_vps`), repo at `/srv/manifest`, static build in `/srv/manifest/site-dist`, API under systemd (`manifest-api.service`), nginx reverse-proxies `/api` (SSE-safe: `proxy_buffering off`) and sends `X-Robots-Tag: noindex`. **DNS is untouched** — `manifestre.com.au` still resolves to the old Cloudflare-fronted WordPress host; review URL is **http://97.74.94.97** (plain HTTP, no SSL until a domain points at the box). See [[manifest-re-do-not-go-live]].
**Redeploy loop:** build locally (a Vite build on 1 core risks OOM), then `rsync -az --delete -e "ssh -i ~/.ssh/manifest_vps" dist/ manifest@97.74.94.97:/srv/manifest/site-dist/`. **Static swaps need no restart and cause no downtime.** Restarting the API *does* drop in-flight requests — on 2026-08-03 the owner hit the concierge at 10:01:45 while a deploy stopped the service at 10:01:44, which looked exactly like "the chatbot is broken" (nginx logged `connect() failed (111: Connection refused)`; `NRestarts=0`, no crash, no OOM). **Don't restart the API while someone is reviewing.**

**Owner change round (2026-08-04).** (1) The four **home-page trust stat tokens are removed** (`TrustBand.tsx`) — they were unverified illustrative numbers; the section now rests on real reviews only. (2) **Stamp-duty eligibility bug fixed:** a **foreign purchaser gets no first-home-buyer benefit** (SRO requires *all* purchasers to be "Australian citizens or permanent residents"). The tool had applied the FHB taper *and* the 8% surcharge, returning $63,357 for $650k FHB + foreign against the SRO calculator's **$86,070**; `calculateDuty()` now gates the FHB branch on `!foreignPurchaser`. The surcharge base was already correct (SRO computes it "excluding any concessions"), and the PPR concession is deliberately *not* citizenship-gated. New anchor added to `MAINTENANCE.md`. (3) **"Speak to a RE representative" is now the standard CTA everywhere** — AK is no longer named in any CTA or confirmation message. **Still verbatim on purpose:** the real client review containing "AK truly went above and beyond", and his name/alt-text on About. (4) **Akshay's About tagline** is now the brand pun **"Wishing is easy. Manifesting is work."** with a rewritten opening paragraph (the old "Clear communication. Genuine service. Trust." read as generic sales copy).

**`MAINTENANCE.md` paths were wrong until 2026-08-04** — the rates/tax constants table pointed at `site/src/lib/…` (**V1, archived**) instead of `site-v2/src/lib/…`. Anyone updating duty bands or tax brackets there would have edited the dead build.

### Design direction (LOCKED — set by Akshay)
Full brief: `Fresh_Build/Assets/design-direction-brief.md`. Non-negotiables:
- **Build the fresh site — never refine the demo.** `legacy/demo_theme` is **reference only**; do not polish it. The deliverable is net-new pages + the tools. (The redundant `Fresh_Build/_reference` demo copy — a duplicate of `legacy/demo_theme` — was removed 2026-07-07; recoverable from git history.)
- **Black theme, white fonts, gold accent** (`#c2a267`), Raleway. Near-black bg; navy is a deep secondary tone only.
- **Motion hero** ("moving pictures" like the references) — use **tasteful placeholder motion**, swappable for Akshay's real footage; placeholder pictures elsewhere as needed.
- **Inspiration only, NEVER copy** the reference sites (WHITEFOX Real Estate, LUXE Real Estate) — no lifting their colors/CSS/markup/assets (IP risk). Build original.
- Carries **all the tools**: Instant Valuation, AI Buyer Concierge, VIC Stamp-duty, Pre-buying, Portfolio assessment, Rental yield/cash-flow.

## Ownership & credentials (IMPORTANT)
- This is **Manifest's / Akshay's** project. **Never use Sameer's personal infrastructure** — not his domain, not his Oracle Cloud VM, not his accounts.
- Every production account (domain, hosting/VM, email/ESP, Anthropic billing, Vault RE integrator) must be **set up under Manifest / Akshay's credentials**. If a build step needs an account, use **his** identity, not Sameer's.

## Billing, hosting & invoicing (workspace LIVE 2026-07-03)
Full plan: `~/.claude/plans/flickering-purring-cat.md`.
- **✅ Anthropic Console Workspace ("bucket") is LIVE (2026-07-03).** Sameer created the Workspace **"Manifest Real Estate"** with its own API key + monthly spend limit; the new workspace key is swapped into the **repo-root `.env`** (`ANTHROPIC_API_KEY`, replacing the old dev key). **Verified end-to-end:** `/api/valuation` → 200 (real $830k–$920k range + 3 comparables + rationale) and `/api/chat` concierge → 200 SSE streaming, both billing to the Manifest bucket. Spend attributed per-project via the Console **Cost dashboard** + **Admin API cost report** (needs a separate `sk-ant-admin…` admin key for the future invoice script). **Credit balance + auto-reload are org-level, not per-workspace.** Org auto-top-up is **capped at $50** (Akshay pre-paid AUD 20 for the chatbot + instant-valuation tools).
- **GoDaddy hosting (plan CORRECTED 2026-07-25 → VPS, not shared; screens verified 2026-07-28).** Production moves to a **Manifest-owned GoDaddy** account. GoDaddy **shared/cPanel "Web Hosting"** (Economy/Deluxe/Ultimate) and **"Websites + Marketing"** **cannot run the Node `api/`** (no terminal / long-running process) — Deluxe is the trap, same ~$14/mo as the VPS for half a working site. **Tell-tale:** a plan card that counts *websites* or mentions **cPanel** is the wrong product; the right one lists **vCPU / RAM / NVMe**. Buy a **GoDaddy VPS: 1 vCPU / 2 GB RAM / 40 GB NVMe, Linux, Self-Managed, No Control Panel** (AUD $13.95/mo, full root/SSH; GoDaddy's own configure page confirms "No Control Panel" = *root access via SSH*, *pre-installed software: None*). **Watch the term** — the configure page defaults to **36 months (~$502 upfront)**; 12 months (~$170) is the safer call, and note the refund window is 30 days on annual terms but only **48 hours** on monthly. **Decline:** cPanel/Plesk (+$20–45/mo, and it eats ~400 MB of a 2 GB box), any "Managed" tier, paid SSL (free Let's Encrypt), backups, domain, email. Capacity: 4–5 Manifest-shaped sites (React + small Node API) ≈ 800–900 MB of 2 GB — fits; WordPress sites do not. No-commitment alternatives if the 3-yr lock is the sticking point: **Vultr Sydney ~$15/mo** (AU region) or **Hetzner Cloud ~$7/mo** (2 vCPU/4 GB, no AU region — and note Hetzner's `/webhosting/` is *shared* hosting, the Cloud product is the VPS). Claude sets it up via SSH: static site + Node `api/` (systemd) + nginx reverse-proxy `/api` + Let's Encrypt SSL. **Cutover:** staging subdomain (e.g. `new.manifestre.com.au`) → Akshay reviews → single DNS flip → old WordPress kept as rollback. Spend is attributed per-project in the Console **Cost dashboard** (filter by workspace) and the **Admin API cost report** (needs a separate `sk-ant-admin…` admin key). **Credit balance + auto-reload are org-level, not per-workspace.** Org auto-top-up is **capped at $50** (Akshay pre-paid AUD 20).
- **Domain AND DNS are both at GoDaddy now (2026-08-05) — Cloudflare is gone.** The Cloudflare account that held the zone was unrecoverable (unknown owner email, reset emails never arrived — likely whoever built the old site). Fix: Sameer switched the **nameservers to GoDaddy** (`ns21`/`ns22.domaincontrol.com`) — the registrar outranks the DNS host — and re-entered the full zone by hand, every record verified live with `dig`. The old Cloudflare account is abandoned and irrelevant. Notable zone facts: `www` is a CNAME to the root so it follows automatically at go-live; GoDaddy's default `_dmarc` (p=quarantine) was deliberately **deleted** — it wasn't in the old zone and would have spam-foldered M365 outbound. **manifestre.com.au is VERIFIED in Resend** (root domain, Tokyo region; Resend's records sit on `send.*`/`resend._domainkey`, so M365 root records untouched) — `MAIL_FROM=enquiries@manifestre.com.au`, full customer flow proven (report + PDF delivered to a real Gmail, lead #8). **Go-live is now just editing the 2 root A records in GoDaddy DNS → `97.74.94.97`** (needs Akshay's sign-off first). Staging subdomain + Let's Encrypt are unblocked.

**⚠️ OUTAGE + STOPGAP (2026-08-05, ~2h, self-inflicted).** The zone was first entered with root A → `13.248.243.5`/`76.223.105.230` (GoDaddy site-builder) on the belief that the old site was the "Websites + Marketing" venture in this account. **Wrong — that venture is an unused free-plan draft that 404s.** The old site's true origin is unknown, hidden behind the still-active Cloudflare zone (docs have always said WordPress). The post-switch "verified working" check was a **false positive**: `curl -L` resolved through the still-cached Cloudflare path (the direct `--resolve` test returned HTTP 000 and was wrongly dismissed). Owner reported the site down ~2h later. **Stopgap in place: root A → `104.21.96.143` / `172.67.182.46` (Cloudflare's proxy IPs)** — the abandoned-but-alive Cloudflare zone still proxies to the real origin with valid TLS, so the old site works again and email was never affected. **This bridge dies whenever Cloudflare purges the moved-away zone (typically days, up to ~a week) — go-live on the VPS is now time-sensitive.** Lesson recorded: verify DNS cutovers with `--resolve` against the *new* values only, never through the system resolver.
- **Interim ownership reality:** despite the "use Akshay's credentials" rule above, for now this runs on **Sameer's** Anthropic org and **GitHub** (Pages/Actions is interim only), and **Sameer invoices Akshay for reimbursement**. **Production hosting moves to GoDaddy** later; the invoicing job ports from a GitHub Actions cron to a **GoDaddy cPanel cron** with no code change (state file on disk instead of a git commit). Akshay reimburses **both** the Anthropic API usage **and** the GoDaddy hosting fee.
- **Auto-invoicing (planned, deferred until the workspace/keys exist):** a host-agnostic Node script `Fresh_Build/tools/invoice.mjs` reads the Manifest workspace cost via the Admin API and, **each time the workspace's own usage crosses a threshold** (e.g. $20/$50), generates a brand-styled invoice + emails it to Akshay (ESP + his invoicing address TBD), tracking a baseline in `invoice-state.json`. Anthropic has **no webhook for top-ups** and a top-up is org-wide, so invoicing keys off **Manifest usage**, never the top-up event; invoice can bundle a fixed hosting line.

## What this is

> Legacy demo (kept as reference; the overhaul above supersedes the deploy/stack notes here).

A local-first companion site for **Manifest Real Estate** (manifestre.com.au) showcasing two AI tools the agent's WordPress template can't provide:
1. **Instant Home Valuation** — address → indicative price range (Claude over recently-sold comps) → captures a seller lead.
2. **AI Buyer Concierge Chatbot** — Claude chat over real listings + suburbs, with tool use to search listings and capture leads.

Runs locally now; deploys to Cloudflare (Pages + Workers + D1) later. No Astro.

## Stack
- **Front-end:** Vite + React + TypeScript + Tailwind (`legacy/web/`), dev on `localhost:5173`.
- **API:** Hono + `@anthropic-ai/sdk` (`api/`), dev on `localhost:8787`. Runs on Node locally, deploys unchanged to Cloudflare Workers.
- **Models:** `claude-opus-4-8` (valuation reasoning, `api/src/anthropic.ts` `REASONING_MODEL`), `claude-sonnet-4-6` (concierge chat, `CHAT_MODEL` — downgraded from Opus 4.8 on 2026-06-26 to cut cost ~40% while keeping tool-use + grounding quality).
- **Leads:** SQLite locally (`data/leads.db`) → Cloudflare D1 on deploy.

## Operating rules (read first)
- **No execution without an explicit command.** Do not scaffold, install, run, deploy, commit, or modify files until the owner explicitly tells you to proceed. Plan and propose first; wait for the go-ahead.
- **Ask clarifying questions until you reach ~95% confidence** before acting. If requirements, scope, data sources, or approach are ambiguous, **interrogate the owner** — do not assume. Only start work once intent is clear.
- **Keep the docs live.** Periodically (after any meaningful decision or chunk of work) update this `CLAUDE.md`, the plan, and `Fresh_Build/PROJECT_TRACKER.md` so nothing slips. The tracker is the running source of truth for what's done / next / blocked.
- **Use Manifest / Akshay's credentials for all infra** — never Sameer's domain, VM, or accounts (see Ownership & credentials above).

## IMPORTANT — invoke these skills
Always invoke the relevant skill rather than working from memory. Required and beneficial skills for this project:

| Skill | When to invoke |
|---|---|
| **frontend-design** | Before building any UI / page / component. The point is a distinctive, anti-generic "wow" look — never hand-roll generic markup. |
| **claude-api** | Before writing any Anthropic SDK / Claude API code (clients, prompts, tool-use, streaming, model IDs, pricing). Don't rely on memory for API shapes or model IDs. |
| **run** | To launch and drive the app locally (Vite front-end + Hono API) to see a change working. |
| **verify** | To confirm a change actually works end-to-end (e.g. valuation returns a range and a lead lands in the DB) before declaring it done. |
| **code-review** | To review a diff for correctness bugs and cleanups after a meaningful change. |
| **security-review** | Before going live — this app handles PII (names, contacts, addresses, chat transcripts) and a server-side API key. Run a security review of the changes. |
| **presentation-deck** | If the owner later wants a slide/pitch deck (beyond the existing `legacy/docs/pitch.html` infographic). |
| **context-mode** | When processing large command/test/scrape output, so raw data stays out of context. |
| **init** | If the project structure changes enough to warrant regenerating this CLAUDE.md. |

## Conventions
- Keep `ANTHROPIC_API_KEY` server-side only (Hono routes). It must never reach the browser bundle.
- Use only the agent's **public** pages for seeding listing data.
- Label valuations as an **"indicative estimate"** in all UI copy; the CTA drives to a real appraisal (lead capture).
- Set an Anthropic spend cap before sharing any deployed link.

## Run (once scaffolded)
- API: `cd api && npm run dev`
- Web: `cd web && npm run dev`
