# Manifest Overhaul — Project Tracker

Living status doc. Updated as we go. Plan: `~/.claude/plans/we-need-to-to-sequential-deer.md`.
Last updated: **2026-07-25**.

## ⭐ V2 LOCKED IN (2026-07-03)
Akshay **locked in the deployed V2** (`Fresh_Build/site-v2/`, live on GitHub Pages) as the chosen build — no more backtracking to V1 (`Fresh_Build/site/` is now archived reference only). Active work now: (1) **Anthropic Workspace "bucket"** setup → chatbot + valuation live; (2) **Vault RE integration — LIVE 2026-07-25.** `vault.ts` client + `/api/vault/listings` + front-end wiring built; MRI provisioned the `X-Api-Key` and Akshay generated the per-office Access Token → **14 real Manifest listings now flow to the site.** Remaining: listing-detail page ⬜ + self-host photos + GoDaddy hosting so it works on the live URL.

## Status legend
✅ done · 🟡 in progress · ⛔ blocked (waiting on owner/third party) · ⬜ not started

## V2 — Agency repositioning (2026-07-02)
Akshay's feedback (verbatim: `Assets/akshay-feedback-2026-07-02.md`) repositions Manifest from a first-home-buyer/tools angle to a **boutique full-service agency + advisory**, with **evergreen** copy (no live market conditions). Built as **V2** so V1 is preserved for backtracking.
| Item | Status | Notes |
|---|---|---|
| V2 copy of the site | ✅ | **`Fresh_Build/site-v2/`** (copy of `site/`; moved up out of the demo copy on 2026-07-02), dev on **:5181** (V1 stays in `Fresh_Build/site/` on :5180). Both run side by side |
| Nav restructure | ✅ | Home · Listings · **Services** (mega-menu) · **First home buyers** · **Tools** · About · Contact. New `ServicesMenu.tsx` added; the V1 First-home + Tools menus **kept** (restored 2026-07-02 per Sameer) |
| 5 service pages (evergreen) | ✅ | Residential Sales, Acreage & Lifestyle, Development Projects, Commercial Leasing, Property Advisory — shared `ServicePage.tsx` layout, Akshay's copy |
| Home rewrite | ✅ | Hero "Property. Opportunity. Results." (motion kept, "Results" = gold shimmer); "Our Expertise" + 4 service cards (`ServicesShowcase.tsx`) replaces the tools showcase |
| About rewrite | ✅ | Evergreen story + Mission + 5 Values. **Two directors** (2026-07-02): Akshay Kapoor (AK) + **Rishi Vohra** (bio de-dashed + headshot from his manifestre agent profile `agent_id=28506`, resized to 227KB → `public/rishi-vohra.jpg`); both titled "Director". Values now a **centred flex** (5 boxes, no dangling empty cell). Portraits **slide in from screen edges on click** (`FounderPhoto`, Akshay←left / Rishi→right, reduced-motion safe) |
| Tools verified end-to-end | ✅ | With `cd api && npm run dev` (:8787): Stamp Duty / Borrowing Capacity / Portfolio / Rental are client-side ✅; **Valuation** (200 + LLM rationale), **Concierge** `/api/chat` (200 streaming), `/api/suburbs` (200), `/api/lead` (200) all live. Anthropic key is present in the shell env (not `api/.env`) |
| Contact rewrite | ✅ | "Let's start the conversation" copy |
| 6 tools + FHB journey | ✅ | Kept as **their own nav sections** (First home buyers dropdown + Tools mega-menu, like V1) **and** surfaced under Property Advisory; footer Tools column restored; routes unchanged; floating concierge unchanged |
| Footer | ✅ | Tools column → Services; agency tagline |
| Brand name | ✅ | Kept **"Manifest Real Estate"** (Akshay's copy said "Manifest Deal" — treated as draft) |
| Build/typecheck | ✅ | `tsc --noEmit` clean, `vite build` clean |
| Deployed to GitHub Pages | ✅ | 2026-07-02 — Pages workflow repointed from V1 to V2; **https://samsteve159.github.io/Real_Estate_web/ now serves V2**, V1 deployment retired. Static (no `/api`): AI/lead forms no-op there, deterministic tools work |
| V2 feedback round 2 (2026-07-02) | ✅ | Akshay's edits applied: Development Projects — copy "residential **and commercial** sites", added services **08 Coordinating Finance / 09 Exit Strategy**; Services dropdown DevProjects hook → "Unlock the value of residential and commercial development sites"; Residential Sales — "Settlement Support" → **"First Home Buyer Sales"**; Commercial Leasing — "Tenant Representation" → **"Blue Chip Tenant Sourcing"**; Property Advisory — dropped the 7th area ("Commercial Property Advice") → 6 areas. New **`RecentProjects.tsx`** showcase on Dev Projects + Commercial Leasing, **hidden by default** (`SHOW=false`; refresh ~every 2 months — only Listings live for now). About — removed "Two partners…" hook, enlarged **"Meet the Directors"**; photos moved **below** names, **smaller + equal width** (`max-w-[220px]`), Rishi's headshot leaned left (`objectPosition:30% top`) toward Akshay; "Welcome to Manifest Real Estate, where dreams meet reality." moved to the **About hero**. Contact — removed office address, Servicing → **"Victoria"**, split the single contact field into **separate Phone + Email** inputs |
| V2 feedback round 3 (2026-07-02) | ✅ | **Reviews for both directors on Home** (`TrustBand.tsx`) — grouped per director, 2 each; Akshay's real (his) + **Rishi's 2 real reviews supplied by Akshay** (were placeholders). About — added Rishi's short real review, moved photos below names (equal `max-w-[220px]`). **Rishi headshot re-cropped** tight to subject (800×800, orig backed up in scratchpad) to cut background + light `brightness(.92)` tone-match to Akshay's grey. **Padding fix:** all 12 inner pages `paddingTop` 6rem→9rem (was hiding under the 128px header). **Mobile menu fix:** panel capped to `calc(100dvh − 8rem)` w/ internal scroll so About/Contact are reachable on short screens |
| **Decisions to confirm with Akshay** | 🟡 | (1) "Manifest Deal" vs "Manifest Real Estate" name; (2) **Insights** page — he named it as frequently-changing but gave no copy; not built yet; (3) ~~promote V2 → replace V1?~~ ✅ **V2 LOCKED 2026-07-03**; (4) content for the hidden **RecentProjects** strips |

## Billing, workspace & invoicing (2026-07-02 — planned; setup ~2026-07-03)
Akshay pre-paid **AUD 20** for API tokens (chatbot + instant valuation); he reimburses Sameer by real project usage. Full plan: `~/.claude/plans/flickering-purring-cat.md`. Building deferred until Sameer completes the Console setup.
| Item | Status | Notes |
|---|---|---|
| Anthropic **Workspace** "Manifest Real Estate" ("bucket") | ✅ **live (2026-07-03)** | Created by Sameer; new workspace key swapped into repo-root `.env` (`ANTHROPIC_API_KEY`, replacing the old dev key). **Verified end-to-end:** `/api/valuation` → 200, real range $830k–$920k + 3 `comparables_used` + rationale; `/api/chat` concierge → 200 SSE streaming. All Manifest traffic now bills to the Manifest bucket. Spend limit set. **Still needed for LIVE-on-deployed-site:** host `api/` under Manifest so the tools work on the Pages site (Pages is static, no `/api`) |
| Admin API key (`sk-ant-admin…`) | ⬜ | Org-admin only; stored as a secret for the invoicing job — never committed / never in a bundle |
| Org auto-top-up cap | ✅ | **Capped at $50** (org-level, shared across Sameer's projects; not per-workspace) — user will flag any change |
| Threshold auto-invoicing script | ⬜ | `Fresh_Build/tools/invoice.mjs` (host-agnostic Node): reads Manifest workspace cost via Admin API; **each time Manifest usage crosses a threshold** (e.g. $20/$50) → brand-styled invoice emailed to Akshay; baseline in `invoice-state.json`. No top-up webhook exists / top-ups are org-wide, so it keys off **Manifest usage** |
| Invoice email (ESP) | ⬜ | Provider-abstracted (default Resend); **Akshay's invoicing address + from-address TBD** (user to share); dry-run until then |
| Automation host | ⬜ | **Interim: GitHub Actions** cron (commits state back). **Later: GoDaddy cPanel cron** — production moves to GoDaddy; ports with no code change. Akshay reimburses **API usage + GoDaddy hosting** (optional fixed hosting line on the invoice) |

## Phase 0 — Setup
| Item | Status | Notes |
|---|---|---|
| `Fresh_Build/` skeleton (`website`, `app`, `Assets`) | ✅ | created |
| Asset drop-guides (`Assets/README.md`, `Fresh_Build/README.md`) | ✅ | |
| WhatsApp to Akshay (domain Q + Vault RE email folded in) | ✅ | `docs/akshay-whatsapp.md`; **sent by owner** |
| Logo received | ✅ | `Assets/logo/…Color.png` — navy + gold; matches `demo_theme` palette |
| Newsletter bot blueprint received | ✅ | `Assets/newsletter/` = Girish LinkedIn-bot pattern (reuse for Milestone 3) |
| Excel file (formula audit) | ✅ | `2026_CHECK PROPERTY STATS.xlsx` audited — math correct (VIC duty + loan interest verified to the $). Issues: orphaned `#REF!` named ranges (unused, harmless now); FHB stamp-duty concession not wired up. Duty tables reusable for the stamp-duty tool |
| Domain name confirmed | ⛔ | asked Akshay via WhatsApp |
| Vault RE API access (key + token) | ✅ | **LIVE 2026-07-25.** `VAULT_API_KEY` (from MRI) + `VAULT_ACCESS_TOKEN` (Akshay generated) both in `.env`; `/api/vault/listings` returns 14 real listings. |
| Anthropic (Claude) account under Manifest | ⏸️ postponed | instructions ready (`docs/akshay-anthropic-setup.md`); revisit later. Local dev uses existing key meanwhile |
| Build the 4 project agents | ✅ | `.claude/agents/`: vault-integrator, manifest-ui, calc-verifier, content-engine |
| Clean broken Excel names | ✅ | 19 `#REF!` names removed → `…STATS (cleaned).xlsx`; values/formatting untouched |
| Design-direction brief (whitefox + luxhabitat crawl) | ✅ | `Assets/design-direction-brief.md`. Crawled WHITEFOX (positioning) + luxury-RE patterns. North star: **premium-agency calm + broker-grade tools + first-home-buyer warmth** — keep navy/gold/Raleway, elevate via whitespace, photography-first cards, tools-as-hero panels |

## Decisions locked
- **Build fresh — never refine the demo.** `legacy/demo_theme` is the reference-only demo copy. Deliverable is a net-new site + the tools. (`Fresh_Build/_reference`, a redundant duplicate of `legacy/demo_theme`, was removed 2026-07-07 — recoverable from git history.)
- **Design (Akshay):** black theme, white fonts, gold accent (`#c2a267`), Raleway. Motion hero ("moving pictures") via tasteful **placeholder motion** (swappable for real footage); placeholder pictures as needed. **Inspiration only, never copy** WHITEFOX / LUXE (IP risk) — build original. Full brief: `Assets/design-direction-brief.md`.
- Foundation: **custom React** (Vite + React + TS + Tailwind), local-first / host-agnostic.
- Hosting target (Cloudflare vs VM) **deferred** to go-live — and must be **Manifest/Akshay's**, not Sameer's.
- **All infra/accounts under Manifest/Akshay's credentials** (domain, VM, ESP, Anthropic billing, Vault). Never Sameer's.
- Repo: `Fresh_Build/` is net-new; old `demo_theme/web/api/docs` kept as reference.
- Hard cap ≤ **$500/mo**.

## Milestone 1 — Live website + Vault RE listings
| Item | Status |
|---|---|
| Vault RE (MRI) API access | ⛔ **integrator form SUBMITTED 2026-07-08 — awaiting MRI review/approval** ("We will reach out shortly to discuss your requirements"). **Correction:** Erwin's WhatsApp "just email provisioning, they do the rest" was wrong. MRI (Joi Usi, Vault Provisioning) closed our email ticket #692058 (and Akshay's separate case #689737) confirming the **developer/integrator must complete the registration form**. **Two-step:** (1) Sameer fills the initial form at `https://www.mrisoftware.com/au/products/vault/api-integrations/` → (2) receives an email (**check spam**), clicks **"Fill Out Form"** → second page of details → submit → Provisioning **reviews + approves internally → issues the API key**. Answer sheet prepped: `Assets/vault-integrator-form-answers-2026-07-08.md`. Register org = **Manifest Real Estate**, developer contact = Sameer; scopes listings read + contacts write; region ap-southeast-2, base `https://ap-southeast-2.api.vaultre.com.au/api/v1.3/`. Help line if stuck: Joi Usi 1300 788 689 (AU). **✅ 2026-07-25: MRI provisioned the `X-Api-Key`** (email to Sameer + Akshay; Integration name "Manifest Real Estate", Office "Manifest Real Estate (5478)") → in repo-root `.env` `VAULT_API_KEY`. **`vault.ts` client + `/api/vault/listings` + live front-end wiring now BUILT & verified.** **Remaining:** Akshay generates the per-office **Access Token** (VaultRE → Office Integrations → Third-Party Access → select the "Manifest Real Estate" integration → Create Token) → `VAULT_ACCESS_TOKEN` in `.env` → listings go live. Verified our request shape reaches Vault correctly (returns Vault's own 401 until the token is added; key alone is not enough). |
| `vault.ts` API client (auth, fetch, normalize, cache) | ⬜ (blocked until the API key + access token above land) |
| Scaffold `Fresh_Build/website/` from `demo_theme` baseline | ✅ runs on :5175 (proxies API :8787); logo wired; renders OK |
| Design system (tokens/type from brief + logo) | ✅ | black/white/gold dark token set live in `site/src/index.css`; logo recolored for black + wired into nav |
| Pages: Home, Listings, Listing detail, About/Agents, Contact | 🟡 | Home, Listings (mock), About (real Akshay photo ✅), Contact ✅. Listing-detail page ⬜ (waits on Vault RE shape) |
| AI Buyer Concierge over real listings | ✅ (mock) | `routes/ConciergePage.tsx` + `streamChat` in `site/lib/api.ts` → existing `/api/chat` SSE (tool-use: search_listings + capture_lead). Full-page dark chat, suggestions, lead capture. Runs over mock listings now; swaps to Vault with no UI change. **Model: `claude-sonnet-4-6`** (downgraded from Opus 4.8 to cut cost ~40%; valuation still Opus 4.8). Live test needs `ANTHROPIC_API_KEY` in `api/.env` |
| Lead capture → DB | ✅ | valuation + contact forms POST `/api/lead` → SQLite; contact tagged `source:"contact"` |

## Milestone 2 — Client tools
| Tool | Status | Notes |
|---|---|---|
| Instant Valuation | ✅ | `routes/ValuationPage.tsx` + `lib/api.ts` → existing `/api/valuation` (now mismatch-fixed). Suburb dropdown, comps, lead capture. Dark restyle |
| Portfolio assessment | ✅ | `lib/portfolio.ts` + `routes/PortfolioPage.tsx`. Multi-property value/debt/equity/LVR, **usable equity** (80% rule), gross yield. Deterministic, no API |
| Rental yield & cash flow | ✅ | `lib/rental.ts` + `routes/RentalPage.tsx`. Gross/net yield, operating expenses, interest-only cash flow, positive/negative gearing verdict. Deterministic, no API. 6th tool added to mega-menu/footer/mobile/showcase |
| Borrowing Capacity Calculator (was Pre-buying) | ✅ | engine `site/src/lib/preBuying.ts` + UI `routes/PreBuyingPage.tsx`. Borrowing capacity (ATO 2024-25 tax → net income → surplus serviced at rate+3% APRA buffer), deposit/LVR, **indicative LMI** by LVR band, full upfront costs (reuses verified stamp-duty engine). Verdict banner + max-purchase. Indicative-only labelling. **Renamed from "Pre-buying" (display only). Sliders → text boxes. Buyer-type "Home to live in"→"Next home". Added "Email me my report" lead-capture CTA + sample HTML/PDF report in `Assets/`.** Typechecks + builds clean |
| VIC Stamp-duty calculator | ✅ | deterministic engine `site/src/lib/stampDuty.ts` + UI `routes/StampDutyPage.tsx`. Rates pulled live from sro.vic.gov.au (general/non-PPR, PPR, FHB). **FHB exemption + concession built** (≤$600k=$0; $600,001–$750k phase-in). Foreign-purchaser 8% toggle. Verified vs SRO anchors: $600k=$31,070 ✓, PPR $500k=$21,970, FHB $650k=$11,356.67. Typechecks clean |

## Milestone 3 — Trust engine
| Item | Status |
|---|---|
| Newsletter engine (adapt Girish bot pattern) | ⬜ |
| Monthly per-client market-update emails | ⬜ |

## What's left (as of 2026-07-28)

### 1. Build work — no server needed
| Item | Status | Notes |
|---|---|---|
| **Listing detail page** (`/listings/:id`) | ✅ 2026-07-28 | Cards on `/listings` and the homepage preview now link to a full property page: gallery + thumbnail strip, price/meta, description, land size, agent card, enquiry form. Built `api/src/vault.ts` `getVaultListingDetail()` + `GET /api/vault/listings/:id`; front-end `getVaultListingDetail()` in `lib/api.ts` (mock ids resolve locally, everything else hits the live endpoint). **Verified against all 14 real listings** — description, land size and agent (name/phone/email) all confirmed against a live response and one field-name bug fixed (`landArea.units` not `.unit`; `contactStaff[]` not `listers[]` — Vault's real shape differed from the first guess). |
| **Self-host listing photos** | ✅ 2026-07-28 | New `api/src/vaultPhoto.ts`: every photo URL (grid + gallery) is rewritten to `/api/vault/photo?src=...`, which downloads from Vault on first request and serves from local disk (`data/vault-photos/`, gitignored) after — browsers never hit Vault's CDN. Host-allowlisted to `*.vaultre.com.au` so it can't become an open image proxy. **Verified**: cold fetch downloaded a real 1.68 MB photo, warm fetch served byte-identical from cache, a non-Vault URL was rejected (404). |
| **Enquiry notification emails** | ✅ built, ⬜ needs a Resend account | New `api/src/email.ts` — admin notification + auto-reply, wired into `POST /api/lead` (fire-and-forget, never blocks or breaks the lead response). Gracefully no-ops when `RESEND_API_KEY` is unset (same pattern as `vault.ts`): **verified live** — a test lead saved correctly and logged `RESEND_API_KEY not set — lead #5 saved but no email sent`. To switch on: sign up free at resend.com (3k/mo), set `RESEND_API_KEY` + `LEADS_NOTIFY_EMAIL` in `.env` (see `.env.example`, now current — it was stale, missing the Vault vars entirely, also fixed). |
| Report delivery by email | ✅ built **incl. real PDF**, ⬜ needs the same Resend account | The "email me my report" CTA sends a branded HTML email **with a generated PDF attached** (`api/src/report.ts`, PDFKit — chosen over headless Chrome because Puppeteer would cost ~300 MB resident per render on a 1 vCPU / 2 GB box). `POST /api/lead` accepts a structured `report` object, clamped through the same guards as every other input; if rendering ever fails the email still goes out without the attachment. **Verified 2026-08-03**: both variants render as valid A4 PDFs (navy letterhead, gold rule, headline figure, serviceability banner, figures + inputs tables, disclaimer). |
| DNS records on `manifestre.com.au` | ✅ **2026-08-05 — customer email fully LIVE** | Root domain verified in Resend (Tokyo region; Resend's rows sit on `send.*`/`resend._domainkey`, root M365 rows untouched). `MAIL_FROM=Manifest Real Estate <enquiries@manifestre.com.au>` local + server. Proved end-to-end with **lead #8**: report email + generated PDF delivered to a real Gmail, admin notification delivered. DNS itself now lives at **GoDaddy nameservers** — see the 2026-08-05 changelog for the full takeover story. |

### 2. Hosting — ✅ SERVER BOUGHT + DEPLOYED (2026-08-03), NOT live
**GoDaddy VPS purchased and fully deployed.** `manifest-prod`, **97.74.94.97**,
Ubuntu 24.04.4 LTS, 1 vCPU / 1.9 GB RAM / 38 GB disk, Singapore region, monthly
term (no lock-in). Admin user `manifest`, passwordless sudo, **key-based SSH**
(`~/.ssh/manifest_vps` on Sameer's Mac — password never shared; installed via
`ssh-copy-id` run by Sameer).

**Stack installed:** Node 22.23.2 (NodeSource) · nginx 1.24 · ufw (only 22/80/443
open; the API port 8787 is loopback-only) · repo cloned to `/srv/manifest` ·
`.env` copied with `chmod 600` · static build rsynced to `/srv/manifest/site-dist`
(built locally — a Vite build on 1 core risks OOM) · API under **systemd**
(`manifest-api.service`, `Restart=always`, enabled at boot) · nginx serves the
static site, reverse-proxies `/api` (SSE-safe: `proxy_buffering off`,
300s read timeout), SPA fallback to `index.html`, and sends
`X-Robots-Tag: noindex` so the staging box stays out of search results.

**Verified working on the server (not just locally):** homepage + SPA deep links
200 · `/api/health` · **14 live Vault listings** · listing **detail** endpoint
(land size, agent name/phone from Vault) · **self-hosted photo proxy** (real
1.68 MB PNG served; non-Vault host rejected 404) · **AI valuation** (Yarraville
3br → $1.12M–$1.25M, confidence high, 3 real comparables) · **AI concierge**
SSE streaming real listing data · **lead capture** (leadId returned). Memory
after all that: **546 MB used of 1.9 GB** — confirms the 4–5-site capacity
estimate. Survives restart + reboot (both services `enabled`).

**NOT LIVE, by instruction.** `manifestre.com.au` still resolves to the old host
(172.67.182.46 / 104.21.96.143 — Cloudflare-fronted). DNS untouched. Review URL
is **http://97.74.94.97** (plain HTTP — no SSL yet, since Let's Encrypt needs a
domain pointed at the box). See [[manifest-re-do-not-go-live]].

**Remaining before go-live (updated 2026-08-05):** (a) ~~Resend/email~~ ✅ done —
customer email fully live; (b) **Akshay's sign-off** (he's reviewing on
http://97.74.94.97); (c) optional: staging subdomain `new.manifestre.com.au` +
Let's Encrypt for a padlocked review link — now unblocked since DNS is ours at
GoDaddy; (d) the flip itself, **now trivial**: edit the 2 root A records in
GoDaddy DNS (13.248.243.5 / 76.223.105.230 → 97.74.94.97, `www` CNAME follows
automatically). Old site stays reachable at manifestre.godaddysites.com as
rollback. (e) **⏸ PDF redesign awaiting owner decision** — draft in
`Fresh_Build/Assets/report-redesign-draft.{mts,pdf}`; live PDF still navy.

**Redeploy loop (in use since 2026-08-03).** Build locally, then
`rsync -az --delete -e "ssh -i ~/.ssh/manifest_vps" dist/ manifest@97.74.94.97:/srv/manifest/site-dist/`.
Static swaps need **no service restart** and cause no downtime. Restarting the
API *does* drop in-flight requests: on 2026-08-03 the owner hit the concierge at
10:01:45 while a deploy stopped the service at 10:01:44, producing a bogus
"chatbot is broken" report (nginx logged `connect() failed (111: Connection
refused)`; `NRestarts=0`, no crash, no OOM). **Avoid API restarts while someone
is reviewing**, or add a reconnect retry to `streamChat`.

### 2b. Hosting — options as priced 2026-07-28 (kept for reference)
- **The gate.** Valuation, concierge, all forms and live Vault listings need the Hono `api/` hosted. They no-op on the static Pages preview by design.
- **Options priced 2026-07-28:** GoDaddy VPS 1 vCPU/2 GB/40 GB **$13.95/mo but on a 3-yr term** (~$500 committed, renews $22.95; 30-day refund on annual terms, 48 hrs on monthly) · **Vultr Sydney ~$15/mo, no commitment, AU region** · **Hetzner Cloud ~$7/mo, 2 vCPU/4 GB, no commitment, no AU region**.
- **Not GoDaddy shared/cPanel "Web Hosting" or "Websites + Marketing"** — they store pages, they can't run software. Rule of thumb: if the plan card counts *websites* or mentions *cPanel*, wrong product; the right one lists *vCPU / RAM*.
- **Decline at checkout:** cPanel/Plesk (+$20–30/mo, more than the server, and eats ~400 MB of a 2 GB box), Managed tier, paid SSL (we use Let's Encrypt), backup add-on, domain, email.
- Capacity check: 4–5 Manifest-shaped sites (React + small Node API) ≈ 800–900 MB of 2 GB — fits. WordPress sites do not; those want 4 GB.
- Buy-sheet artifact for Akshay: https://claude.ai/code/artifact/0f103fca-ed52-4dd1-ad40-375a899cd360 (private by default — hit Share before sending).

### 3. Deploy sequence once a server exists
Install Node + nginx → deploy static build + `api/` under systemd → nginx reverse-proxy `/api` → Let's Encrypt SSL → **staging subdomain** (`new.manifestre.com.au`) for Akshay's review → single DNS flip. Old WordPress stays live as rollback throughout.

### 4. Still outstanding elsewhere
- **Real assets to swap:** Akshay's hero footage (`hero.mp4`) and/or a real B&W skyline photo; white/mono logo SVG if available.
- **Milestone 3 (separate from the website):** newsletter + monthly market-update engine via the content-engine agent.
- **Auto-invoicing script** (`Fresh_Build/tools/invoice.mjs`) — needs an `sk-ant-admin…` key.
- Optional: visual QA pass of the 6 tools + mega-menu + floating bot.

## Open questions for owner
- ~~**Hosting provider + term**~~ — ✅ **resolved**: GoDaddy VPS bought on a **monthly** term (no 3-yr lock), deployed 2026-08-03.
- **Who owns the Resend account** — per the ownership rule it should be Manifest/Akshay's, since the ESP will hold client PII (names, emails, phones, addresses). Currently unowned; nobody is emailed until it exists.
- **Where enquiry emails should land** — one `LEADS_NOTIFY_EMAIL` currently receives **every** form (contact, listing enquiry, valuation, concierge, report). Needs an address Akshay actually watches. Open design question: should **listing** enquiries instead route to that listing's agent? Vault already gives us `contactStaff[]` with their email, so it is a small change if wanted.
- **Whether Akshay authorises the DNS records** on `manifestre.com.au` — access exists, permission should be explicit. Note this is now a **hard blocker on all enquiry email**, not a nice-to-have (see §1). Using the `send.` subdomain means nothing his live site depends on is touched.
- **Auto-reply wording** — it currently promises "AK will personally review your enquiry", which reads oddly now Rishi is also a director and the rest of the site says "a RE representative".
- Production account ownership specifics (Anthropic billing, hosting, ESP) — confirm all under Manifest/Akshay.
- Excel: what is it meant to calculate? (audit once received).
- **Who owns calculator maintenance** (tax brackets, stamp-duty schedule, APRA buffer) after handover — see `Fresh_Build/docs/MAINTENANCE.md`. Recommend codebase-maintainer-owned on an annual/post-budget cadence; Akshay to confirm.

## Changelog

- **2026-08-05 late (⚠️ ~2h outage of the old site + stopgap + runbook)** — The takeover's root A records pointed at the GoDaddy site-builder on a **wrong origin assumption** (that venture is an unused free-plan draft; the real old site is WordPress at an unknown host behind Cloudflare). The post-switch verification was a **false positive** (`curl -L` rode the cached Cloudflare path; the direct `--resolve` HTTP 000 was wrongly dismissed). Owner caught it ~2h in; **stopgap**: root A → Cloudflare's proxy IPs (`104.21.96.143`/`172.67.182.46`) — the abandoned zone still proxies the real origin with valid TLS. Old site restored ~30 min; **email unaffected throughout**. Bridge dies when Cloudflare purges the moved-away zone (days → ~a week) → **go-live is TIME-SENSITIVE; chase AK's sign-off**. Full postmortem + the step-by-step cutover procedure (incl. the two easy-to-forget items: strip the staging `X-Robots-Tag: noindex` from nginx, and certbot immediately after the flip): **`Fresh_Build/docs/DNS-INCIDENT-2026-08-05-AND-GOLIVE-RUNBOOK.md`**.
- **2026-08-05 (DNS takeover · customer email LIVE · wording sweep · PDF redesign draft)** — (1) **DNS moved to GoDaddy nameservers; Cloudflare abandoned.** The Cloudflare account holding the zone was unrecoverable (unknown owner email — reset to admin@ never arrived; almost certainly whoever built the old site). Owner switched NS to `ns21/ns22.domaincontrol.com` at the registrar and re-entered the whole zone hand-held row by row, each record verified live with `dig` against GoDaddy's NS: 2×A root → GoDaddy site-builder (the "old WordPress" was in fact **Websites + Marketing in the same GoDaddy account**, `manifestre.godaddysites.com`), M365 MX/SPF/NETORG TXT, 4 CNAMEs, 2 SRVs, 3 Resend rows. GoDaddy's auto-added `_dmarc` p=quarantine was **deleted** (not in the old zone; would have spam-foldered M365 outbound). Old site + company email re-verified working after the move. (2) **manifestre.com.au VERIFIED in Resend → customer email fully live.** `MAIL_FROM=enquiries@manifestre.com.au`; **lead #8 proved the full flow**: report email + generated PDF delivered to a real Gmail, admin notification alongside. **Go-live is now just editing the 2 root A records → 97.74.94.97.** (3) **Wording:** all 15 "RE representative" → **"Manifest representative"** (site + auto-reply email), plus ContactCTA's "Manifest agent" ×2 caught by the owner's follow-up question (`841a90c`, `dc9692b`). (4) **PDF redesign draft** per owner ("Manifest branding rather than the blue stuff" + directors' photos top-right): near-black letterhead, white badge logo, both directors circular w/ gold rings, site gold accents, warm neutrals — **draft only**, `Fresh_Build/Assets/report-redesign-draft.{mts,pdf}` (run the .mts from `api/` so pdfkit resolves), emailed to the owner for approval. **⏸ PAUSED awaiting owner's decision; live PDF unchanged (still navy).**

- **2026-08-04 (owner change round: trust tokens, stamp-duty bug, CTA sweep, AK bio)** — (1) **Home page trust tokens removed** (`TrustBand.tsx`): the four stat blocks (100+ clients guided, 8 yrs, 4 service lines, 1 call) are gone at the owner's request. They were flagged in-code as illustrative-until-confirmed, so this also removes four unverified claims; the section now rests entirely on real client reviews. (2) **Stamp-duty correctness bug found by the owner and fixed** — at $650k, first home buyer, foreign purchaser the tool returned **$63,357** against the SRO calculator's **$86,070**. Cause: the FHB taper was applied *and* the 8% surcharge added. Per SRO eligibility, *all* purchasers must be "Australian citizens or permanent residents", so a foreign purchaser gets **no** FHB benefit; `calculateDuty()` now gates the FHB branch on `!foreignPurchaser` and explains why in the `basis` string. Verified $86,070 exactly ($34,070 general + $52,000 surcharge); all three `MAINTENANCE.md` anchors re-checked unchanged (general $600k → $31,070, PPR $500k → $21,970, FHB $650k domestic → $11,356.67), and the new foreign case added as a fourth anchor. Confirmed the surcharge base was already right (SRO computes it "excluding any concessions") and that the PPR concession is deliberately *not* citizenship-gated. (3) **CTA phrase unified to "Speak to a RE representative"** across Stamp Duty, Portfolio, Rental, Borrowing Capacity, First-home steps and Valuation, then **swept further** on request: the concierge/valuation/contact/listing-enquiry confirmations no longer name AK, and the Listings line became "**Manifest** works with a wide network" (rather than "a RE representative works with", which would have gutted the selling point). 14 "RE representative" references site-wide, zero "AK will…". **Left verbatim on purpose:** the real client review containing "AK truly went above and beyond", and Akshay's own name/alt-text on About. (4) **Akshay's About copy rewritten** — "Clear communication. Genuine service. Trust." read as generic sales copy, and the paragraph beneath repeated the same triad, so both changed. Owner picked the Manifest pun **"Wishing is easy. Manifesting is work."** (37 chars, down from a first attempt at 71) — two beats matching Rishi's rhythm, encoding the preparation his real reviews describe. Typecheck + build clean throughout; deployed static-only (no restart, no downtime) and **verified in the deployed bundle**, not just locally. Commits `2548df8`, `fd062cc`, `f699cc3`.
- **2026-08-03 (VPS deploy verified · borrowing-capacity PDF · SSE fix · Resend blocker found)** — (1) **Full stack deployed to the GoDaddy VPS** and verified on the server, not just locally (see §2 for the detail). **Not live** — DNS untouched, `manifestre.com.au` still resolves to the old Cloudflare-fronted WordPress host; review URL is `http://97.74.94.97` with `X-Robots-Tag: noindex`. (2) **Borrowing Capacity report is a real branded PDF** (`api/src/report.ts`), replacing a cosmetic PDF/HTML toggle that sent the same HTML either way. (3) **SSE fix** (`b7542c5`) — the concierge's final event could be dropped because writes weren't awaited. (4) **Resend blocker identified**: their docs require a verified domain you own, so the shared-sender fallback in `email.ts` cannot work and enquiry email stays dark until DNS is added. Free tier confirmed as $0/mo, **3,000 emails/month, 100/day, 1 domain**, 30-day retention.
- **2026-08-03 (GoDaddy VPS bought + full stack deployed — NOT live)** — Akshay bought the VPS (`manifest-prod`, 97.74.94.97, Ubuntu 24.04, 1 vCPU/2 GB, Singapore, **monthly** term so there's no lock-in). Setup snags worth remembering: (1) `ssh-copy-id` failed twice — first `Host key verification failed` (fixed with `ssh-keyscan >> known_hosts`), then `Permission denied` because Claude Code's `!` prefix has **no TTY, so SSH can't prompt for a password**; it only worked once Sameer ran it in a real Terminal.app window. A later `Connection closed` was the server **rate-limiting** after repeated failures, not a bad password. (2) `npm ci --omit=dev` was wrong for this API — `tsx` is a devDependency and the service runs `tsx src/index.ts`, so the full install is required. (3) Ping to the box is ~195 ms, which reads like a US server, but the dashboard confirms Singapore — noted, not acted on. **Deployed:** Node 22 + nginx + ufw (8787 loopback-only), repo at `/srv/manifest`, `.env` at `chmod 600`, static build **rsynced from local** (1-core box would risk OOM on a Vite build), API under systemd with `Restart=always` + enabled at boot, nginx reverse-proxy with `proxy_buffering off` for the concierge's SSE stream, SPA fallback, and `X-Robots-Tag: noindex`. **Verified end-to-end on the server:** 14 live Vault listings, the listing-detail endpoint (real land size + agent), the self-hosted photo proxy (1.68 MB PNG through; non-Vault host rejected), AI valuation (Yarraville 3br → $1.12M–$1.25M, high confidence, 3 real comps), concierge SSE streaming real listings, lead capture. 546 MB of 1.9 GB used. **DNS deliberately untouched** — `manifestre.com.au` still resolves to the old Cloudflare-fronted host; review is at `http://97.74.94.97` (no SSL until a domain points at it). A valuation test returning 0 comparables was **not** a bug: "Reservoir" isn't in the seed data, whose 14 suburbs are Altona North → Winter Valley.
- **2026-07-28 (listing detail page + self-hosted photos + enquiry emails, built + verified)** — Auto-mode build session: the two real gaps identified earlier today (clicking a listing did nothing; enquiries were saved but nobody was emailed) are now closed. (1) **Listing detail page**: `api/src/vault.ts` gained `getVaultListingDetail()` + a new `GET /api/vault/listings/:id`; front-end `ListingDetailPage.tsx` (gallery, meta, description, agent card, enquiry form) at `/listings/:id`; `ListingsPage`/`ListingsPreview` cards now link there. Verified against **all 14 live listings** (200s across the board) and against one full raw Vault response — caught and fixed two wrong field-name guesses (`landArea.units` not `.unit`; `contactStaff[]` not `listers[]`), confirmed via a live property showing real land size (5 acre), real agent (Akshay Kapoor, correct mobile + email pulled straight from Vault). (2) **Self-hosted photos**: new `api/src/vaultPhoto.ts` — every photo URL now routes through `/api/vault/photo?src=...`, which downloads from Vault once and serves from `data/vault-photos/` (gitignored) after; host-allowlisted to `*.vaultre.com.au` to prevent an open image-fetch proxy (SSRF). Verified: cold fetch pulled a real 1.68 MB photo, warm fetch served byte-identical from disk, a non-Vault URL was rejected. (3) **Enquiry emails**: new `api/src/email.ts` (Resend), wired fire-and-forget into `POST /api/lead` so a slow/failed send never blocks or breaks the lead response; sends an admin notification + an auto-reply, and for the Borrowing Capacity "email me my report" CTA sends the actual summary (as HTML — PDF attachment not yet built). Gracefully no-ops without `RESEND_API_KEY`, verified live (lead saved, clean log line, no crash). `.env.example` was stale (missing the Vault vars entirely) — brought current with Vault + the new email vars. Also added a light rate limit to `/api/vault/*` (previously unprotected against Vault's own 10k/day cap). All of api + site-v2 typecheck clean; site-v2 production build clean (80 modules). No browser screenshot this session (Chrome extension not connected) — verified instead via the actual HTTP paths the browser would hit, through the Vite dev proxy on :5181, plus a production build.
- **2026-07-28 (Vault work committed + pushed · hosting priced · Pages outage fixed · domain/DNS found)** — (1) **Committed + pushed the Vault integration** (`08792bc`) — it had been sitting **uncommitted** since 2026-07-25 (`api/src/vault.ts` untracked; `ListingsPage`/`ListingsPreview`/`api.ts`/`mockListings.ts` modified), so it existed only on Sameer's laptop and was never deployed. Site build clean before push (79 modules, 388 kB). Two unpushed app-phase commits went up with it. (2) **GitHub Pages outage diagnosed + fixed** — the repo was briefly made **private**, which **disables Pages on the free plan**; the preview was returning `404 Site not found`. Flipping back to public does **not** re-enable it — Settings → Pages → Source: GitHub Actions had to be re-set, then a push (`d2eb76e`, workflow comment) re-triggered the deploy. **Verified live**: HTTP 200, correct title, deployed bundle contains the Vault fetch code + mock fallback (CSS hash matches local; JS hash differs because CI injects the `/Real_Estate_web/` base path — expected). Repo audited for secrets before going public: `.env` correctly gitignored + never committed, only prose mentions of key *names* in docs. (3) **Hosting researched + priced** (see "What's left"): GoDaddy's cheap **"Web Hosting" / "Websites + Marketing" ruled out** (store pages, can't run the Node `api/`); GoDaddy **VPS page shows a 3-yr term** for $13.95/mo (~$500 committed, renews $22.95) vs **Vultr Sydney ~$15/mo** and **Hetzner Cloud ~$7/mo**, both no-commitment. Hetzner's **`/webhosting/` is shared hosting, not the Cloud VPS** — L/XL do list SSH+Node.js but cap at 384–512 MB, no root. Oracle Cloud free tier ruled out on **ownership** grounds (Sameer's personal account), not capability. Buy-sheet artifact built for Akshay. (4) **Domain + DNS located** — `manifestre.com.au` is registered at **GoDaddy inside the "Manifest Real Estate" account** (renews 2027-01-21, $21.95/yr) and **Sameer has DNS access**. This unblocks the staging subdomain and SPF/DKIM without waiting on Akshay, and **removes the "keep it in one account" argument for buying GoDaddy hosting** (the domain stays there regardless of where the server lives). (5) **Gap confirmed by reading the code:** `api/src/leads.ts` writes submissions to SQLite and **sends no email** — no SMTP/ESP/webhook anywhere; and `ListingsPage.tsx` has **no detail-page link** (its only `<Link>` goes to `/contact`), so clicking a listing does nothing. Both logged as build work.
- **2026-07-25 (Vault RE integration LIVE)** — **Akshay generated the per-office Access Token → `VAULT_ACCESS_TOKEN` in `.env` → listings are LIVE.** Verified end-to-end: `/api/vault/listings` returns **14 real Manifest for-sale listings** (`isLive:true`), normalised cleanly. Refined `normaliseListing()` against the real Vault shape: **state** reads `address.suburb.state.abbreviation` (nested object, was blank), **suburb** title-cased (Vault returns UPPERCASE), **price** cleaned for POA listings via `priceOnApplication`, photos prefer `published`. All 14 render with address/suburb/state/postcode/price/beds/baths/parking/type/status/image populated. (Minor: some street names are UPPERCASE in Vault's own data — cosmetic, left as-is.) **Follow-ups:** build the **listing-detail page** (Vault property id → `/properties/{id}`) and **self-host photos** (Vault forbits CDN hotlinking) before/at GoDaddy deploy.
- **2026-07-25 (Vault RE integration built)** — MRI **provisioned the integrator API key** (email to Sameer + Akshay; Integration name "Manifest Real Estate", Office "Manifest Real Estate (5478)"), added to repo-root `.env` as `VAULT_API_KEY` (+ `VAULT_API_BASE_URL`, `VAULT_INTEGRATION_NAME`, `VAULT_OFFICE_NAME`, and an empty `VAULT_ACCESS_TOKEN` awaiting Akshay). Researched the VaultRE API from the official docs (`docs.api.vaultre.com.au`): two-header auth (`X-Api-Key` + `Authorization: Bearer <access token>`), endpoint `GET /properties/residential/sale?published=true&status=listingOrConditional`, base `…/api/v1.3`, rate limit 10/s + 10k/day (cache, don't call live), no photo hotlinking. **Built:** `api/src/vault.ts` — env-driven client (lazy env reads so dotenv timing is safe), pagination, defensive normaliser → the site listing shape, 10-min in-memory cache, graceful degradation to `null` on any error. **Wired:** new `GET /api/vault/listings` endpoint (`{isLive, listings}`); front-end `getVaultListings()` in `site-v2/lib/api.ts`; `ListingsPage` + `ListingsPreview` now render placeholders on first paint then swap to live data when the API returns it (banner hidden when live), falling back to mock when the API is unreachable (e.g. static Pages build). **Verified:** api + site-v2 typecheck clean; `/api/vault/listings` returns `not_configured` (no token) and `unavailable` (token present but Vault 401) without crashing; a live call confirmed our request shape reaches Vault (real `401 Unauthorized Access`). **One blocker left:** Akshay generates the per-office Access Token (VaultRE → Office Integrations → Third-Party Access → "Manifest Real Estate" integration → Create Token) → paste into `VAULT_ACCESS_TOKEN` → listings go live. **Then:** verify `normaliseListing()` against one real item + build the listing-detail page + self-host photos (no hotlinking).
- **2026-07-07 (About/Home director polish + Vault path + repo cleanup)** — (1) **About "Meet the Directors" order swapped** — Rishi Vohra now first/left, Akshay second/right; **Home `TrustBand` reviews** reordered to match. (2) **Director taglines** added under each name (gold italic three-beat): Rishi "Superior negotiation. Personalised service. Real results.", Akshay "Clear communication. Genuine service. Trust." (3) **Rishi's About review** changed to "Professional, responsive and reliable. He knows exactly how to achieve a high sales result." and **all em dashes stripped** from both directors' quotes (About + Home) + the two About Values lines, per the site-wide no-em-dash convention. All committed + deployed to Pages (green). (4) **Vault RE access path simplified** — per Erwin Pasion (Vault), no integrator-registration form needed; **email the request to `provisioning@vaultre.com.au`** quoting "Manifest Real Estate" and their team provisions it. Email drafted (`Assets/vault-api-request-email-2026-07-07.md`) + **sent by Sameer** (cc Akshay); awaiting key + token. (5) **GoDaddy** — Akshay has the login; **awaiting the plan/tier name** to confirm Node.js support before inspecting/deploying. (6) **Repo cleanup** — removed the redundant **`Fresh_Build/_reference/`** (a duplicate of `legacy/demo_theme`, referenced nowhere, not in the deploy) and the regenerable `legacy/web/tsconfig.tsbuildinfo` cache; docs updated. Safe: deploy is scoped to `Fresh_Build/site-v2/` only, and the deletion is recoverable from git history. `legacy/demo_theme` remains the sole read-only demo reference.
- **2026-07-03 (workspace live · V2 locked/deployed · MRI + GoDaddy planning · Akshay explainer)** — (1) **Anthropic Workspace "Manifest Real Estate" created + LIVE.** Sameer set it up in Console (own key + spend limit); new workspace key swapped into repo-root `.env` (replacing the old dev key). Verified end-to-end on the new bucket: `/api/valuation` 200 ($830k–$920k + 3 comparables + rationale), `/api/chat` concierge 200 SSE. Workspace ID not needed anywhere (key is workspace-scoped). (2) **V2 LOCKED** as the chosen build (Akshay signed off); V1 archived reference. (3) **Valuation loading UX:** replaced "Reading the comparable sales…" text with an on-brand **gold dual-ring spinner** (`site-v2/src/routes/ValuationPage.tsx`). (4) **Deployed** V2 to GitHub Pages (`7ef45f5`); first deploy hit a **transient `actions/deploy-pages` failure** (build+artifact were green) → re-triggered (`2dbef98`) → live, new asset confirmed. (5) **Vault/MRI** access path nailed from MRI docs: needs `X-Api-Key` (our integrator registration) + `Authorization: Bearer <token>` (Akshay generates); base `…api/v1.3/`. The screenshot's 2 tokens are other integrators' (Vault Web Sites, Designly) — unusable. Message to Akshay drafted + sent. (6) **GoDaddy hosting** scoped: static site runs anywhere, but `api/` needs a Node-capable cPanel plan (verify before buy; 30-day refund → VPS fallback); Akshay to buy under Manifest → share cPanel/SSH → Claude inspects + deploys. (7) **Explainer artifact** built for Akshay (`scratchpad/manifest-status.html`) — "opening a new office" analogy + why the AI tools switch on only at launch. Docs (CLAUDE.md, this tracker) + memory updated.
- **2026-06-30 (landing polish + AK branding + nav/scroll fixes)** — Owner-driven UX pass, committed + pushed (`4f4a125`). (1) **Hero headline** (`MotionHero.tsx` + `index.css`): `with conviction.` → **`with Conviction`** (capital C, period dropped); new `.conviction` gold-gradient text that **settles in on load then shimmers** continuously (`conviction-settle` + `conviction-shimmer` keyframes; reduced-motion keeps the static gold). (2) **Lit skyline windows** (`public/hero/melbourne-skyline.svg`): generated **~548 warm gold-toned window rects** across the prominent foreground towers so the skyline reads as a living city at dusk. Static (the SVG is a CSS `background-image`, where CSS/SMIL animation doesn't run) — kept tasteful, not every building. (3) **Akshay → "AK"**: casual CTAs site-wide now use **AK** (ContactCTA, Valuation, Listings, Stamp Duty, Portfolio, Rental, Contact page, concierge "booked in" toasts); the **Borrowing Capacity CTA** changed to **"Speak to a RE representative →"**; About founder heading → **"Akshay Kapoor (AK), Director"** (full name establishes who AK is). **Real review quotes in `TrustBand` left verbatim** + the formal "Akshay Kapoor" review header kept. (Owner floated then **dropped** a "Victoria / free for anyone in Melbourne" stat change — TrustBand stats unchanged.) (4) **Scroll-to-top on navigation** (`Shell.tsx`): every route now opens at the top (`window.scrollTo(0,0, behavior:"instant")` on `pathname` change) — fixes pages opening mid/bottom when navigating from a scrolled position via the dropdowns. (5) **Mobile hamburger menu** (`Shell.tsx`): top-level **Listings / About / Contact** are now **gold by default → white on hover** (new `mobileTopLink` class, separate from desktop `linkClass` so desktop nav is untouched), bumped to `text-base` (a touch larger than the "First home buyers"/"Tools" eyebrows); gold-underline hover animation preserved. Build clean; verified the rendered landing page via headless Chrome screenshot (Conviction headline + lit windows confirmed).
- **2026-06-29 (first-home-buyers section, logo/hero refinements, report CTA + sample report)** — Big owner-driven UX session ahead of sharing the preview with Akshay. (1) **New "First home buyers" nav dropdown** (`components/FirstHomeMenu.tsx`, inserted after Listings) surfacing **Steps to follow + the 3 buyer tools** (Instant Valuation, Stamp Duty, Borrowing Capacity) directly; gave it its **own `fhb-in` keyframes** (the cards reused Tools' `bucket-in`, which only existed when the Tools menu was open → cards stuck at `opacity:0` = the intermittent "sometimes shows" bug) and a full-gap hover bridge + 260ms close delay (same fix applied to ToolsMenu). (2) **New 7-step buyer journey page** (`routes/FirstHomeSteps.tsx`, `/first-home-buyers/steps`): congrats hook **"Congratulations on Manifesting your first home"** (brand play, gold shimmer), gold timeline with pulsing nodes + scroll-reveal, each step a setup line then a gold `→` outcome line; closing hook **"Stop guessing what you can afford / Click below and find it out for free"** (shimmer + scale + bouncing chevron) with buttons **"Access Your Free Tools"** + **"Talk to a Manifest representative"**. Step copy revised per owner (panel lender, full out-of-pocket cost, conveyancer/solicitor review, private-treaty/auction, unconditional→deposit→settlement). (3) **New curated buyer-tools page** (`routes/FirstHomeTools.tsx`, `/first-home-buyers/tools`). (4) **Logo:** recolored the original badge — wordmark + tower columns to **azure** first, then **locked to brand navy `#003970`** per owner (towers + "MANIFEST" navy, "REAL ESTATE" + ring gold; recolor script targets navy pixels via numpy, output `site/public/manifest-logo-color.png`); enlarged to **116px** and **pixel-aligned the circle's right edge to the "FI" of "FIRST"** in the (old) eyebrow; header grown `h-16`→`h-32` (128px) with dropdown offsets + `.section-pad` top `7rem`→`9rem` to clear the taller fixed header. (5) **Hero:** removed the blue tint (neutral near-black gradient in `MotionHero.tsx`), dropped the first-home eyebrow, headline now **"Make your next move / with conviction"**. (6) **Renamed Pre-Buying → "Borrowing Capacity Calculator"** (display only; route unchanged) across mega-menu/showcase/mobile/page; buyer-type label **"Home to live in" → "Next home"**. (7) **All `type="range"` sliders replaced with text boxes** (interest rate in PreBuying; shared `PctField` in Rental → covers interest rate + management fee) — stepper buttons + `%` suffix + free decimal typing via a text buffer. (8) **"Email me my report" CTA** on the Borrowing Capacity tool (`ReportRequest` in `PreBuyingPage.tsx`): name + email + PDF/HTML toggle → POSTs `/api/lead` as `source:"contact", intent:"report"` with the full borrowing summary in the message; **verified end-to-end (leadId returned)**. (9) **Sample report built for the AK discussion** — `Assets/sample-borrowing-capacity-report.html` (brand-styled, print-ready) + generated `Assets/sample-borrowing-capacity-report.pdf`. (10) **Reviews relabeled** (`TrustBand.tsx`) to drop the unverified "realestate.com.au" claim (sourced directly from Akshay). Typecheck + build clean throughout; verified via headless screenshots. **Deploy caveat for Akshay's play-around:** the Pages preview has **no `/api` backend**, so Valuation, Concierge, Contact form **and the new report-request CTA no-op there** — the deterministic calculators (Stamp Duty, Borrowing Capacity, Portfolio, Rental) and the steps page work fully.
- **2026-06-27 (GitHub Pages preview live)** — Stood up a **Sameer-hosted preview deploy** so the site can be reviewed on mobile during the build phase (production hosting still moves under Akshay later). Live URL: **https://samsteve159.github.io/Real_Estate_web/**. Added `.github/workflows/deploy-pages.yml` (GitHub Actions: `npm ci` + Vite build with `VITE_BASE=/Real_Estate_web/`, SPA `404.html` fallback + `.nojekyll`, deploy via `actions/deploy-pages`). Made the app **base-path aware** so assets resolve under the `/Real_Estate_web/` subpath: `vite.config` base from `VITE_BASE` env (stays `/` locally), `BrowserRouter basename={import.meta.env.BASE_URL}`, and prefixed public-asset refs with `BASE_URL` in index.html + Shell/MotionHero/TrustBand/AboutPage. First run failed at `configure-pages` (Pages not yet enabled); Akshay/owner enabled **Settings → Pages → Source: GitHub Actions**, re-pushed → deploy green, live page 200, renders correctly on a 390px mobile viewport. Auto-redeploys on any push touching `Fresh_Build/site/**`. **Caveats:** no `/api` backend on Pages, so AI valuation/concierge + lead forms no-op there (deterministic calculators work); 3 harmless console 404s are the unset hero swap-points (`hero.mp4`, `hero-poster.jpg`, `.jpg` skyline) which fall back to gradient + SVG.
- **2026-06-27 (About bio grounded in Akshay's real profile)** — Owner shared Akshay's actual bio from manifestre.com.au. Rewrote the AboutPage founder copy to match it: dropped the fabricated first-home-buyer origin story; led with the "Manifest = turning manifestations into reality" brand thread, his three pillars (clear communication, service, trust), breadth across the lot (land/houses/development/acreage), his own portfolio as a credibility hook, and the responsive/proactive/referral personality. Added his client pull-quote ("a knack to evolve and become more efficient with the ever-changing market") as a gold-left-border blockquote, **no attribution label** (owner felt "In a client's words" / "A Manifest client" didn't sit right). Top intro left first-home-buyer-framed per owner. Typecheck clean; verified via headless screenshot.
- **2026-06-26 (reviews + agent photo)** — Owner asked to put real client reviews + Akshay's photo on the site. realestate.com.au (the agency review page) is behind Akamai bot protection → `HTTP 429` on every automated fetch; pulled Akshay's headshot instead from `manifestre.com.au/agents` (same photo), resized 800×800 → `site/public/akshay-kapoor.jpg`. (1) **Home `TrustBand`**: replaced the 2 placeholder testimonials with **4 real reviews** owner supplied from realestate.com.au (condensed faithfully for the card layout; the 2 truncated ones completed using only owner's wording — no invented claims). Added a section header with Akshay's avatar + 5-star line; each card shows a gold ★★★★★ row. Attribution kept as **"Verified seller · realestate.com.au"** — confirmed with owner the source page shows no reviewer names, only "Verified seller". (2) **AboutPage** founder block: swapped the "Photo of Akshay" placeholder for the real headshot (4:5, gold top-accent). Typecheck clean; verified both via headless full-page screenshots (CDP, reduced-motion + scroll to trip lazy-load). Committed + pushed (`0a81079`).
- **2026-06-26 (UX + cost pass)** — Owner-requested fixes: (1) **Tools mega-menu hover bug** fixed (`components/ToolsMenu.tsx`) — the `position: fixed` panel was detached from the trigger, so crossing the dead gap fired `mouseleave` and closed it; added a close-delay timeout, panel hover handlers, and a transparent bridge over the gap. (2) **Contact details made real** (`routes/ContactPage.tsx`): admin@manifestre.com.au, +61 403 466 216, 2 Blackwood Drive, Altona North VIC 3025 (email/phone now tappable). (3) **Landing order swapped** (`routes/Home.tsx`) — Current Listings now leads, broker-grade Tools follow (lead with the product, then the differentiator). (4) **Concierge model downgraded Opus 4.8 → `claude-sonnet-4-6`** (`api/src/anthropic.ts`) to cut cost ~40%; Sonnet holds the tool-use + grounding discipline the bot needs. Valuation reasoning stays on Opus 4.8. Typecheck clean. **Needs API restart to take effect.**
- **2026-06-25 (hero refine)** — Owner feedback on the hero: redrew the **skyline with more contrast + character** (varied tops, antennas, Eureka/spire landmarks; no longer a bar graph) and raised opacity to 0.8 so it's clearly visible. **Removed the tool-chip trust strip** under the CTAs and the **scroll indicator + its animation**. Rewrote the subhead to: "Buying or selling property should feel exciting, not overwhelming. With the right agent beside you and broker-grade tools at your fingertips, every step is a confident one." Build clean.
- **2026-06-25 (rental + hero)** — Built the **Rental tool** (`lib/rental.ts` + `routes/RentalPage.tsx`): gross/net yield, operating costs, interest-only cash flow, positive/negative gearing verdict. Wired into route `/tools/rental`, the Tools mega-menu (now **6 buckets**, 3-col grid), mobile menu, footer and home ToolsShowcase (removed the "More coming" filler). Added a **black-and-white Melbourne skyline** to the hero (`public/hero/melbourne-skyline.svg`, greyscale, with a swap point for a real photo at `melbourne-skyline.jpg`). Removed the low-value **"Broker-grade tools, free"** eyebrow from the hero trust strip. Typecheck + build clean.
- **2026-06-25 (nav + UX pass)** — Owner-requested changes: (1) **Tools mega-menu** (`components/ToolsMenu.tsx`) folds the 5 tools into 5 big animated buckets under the "Tools" label (staggered reveal + hover lift; Concierge bucket opens the floating bot). (2) **Floating concierge** (`components/ConciergeWidget.tsx`) mounted globally in Shell, fixed bottom-right, follows scroll; opens via launcher or `manifest:open-concierge` event. (3) Nav: 4 labels pushed right (`ml-auto`), **"Value my home" CTA removed**, mobile hamburger reworked with an expandable Tools group. (4) **Copy made first-home-buyer-centric with strong hooks** (hero "Buy your first home with your eyes open"; hero CTA "See what you can afford"; seller band "Sell with us"). (5) **All em dashes removed** site-wide (22 files swept + artifacts fixed). Typecheck + build clean.
- **2026-06-25 (concierge)** — Built the **AI Buyer Concierge** page on the new site: full-page dark chat (`routes/ConciergePage.tsx`) over the existing `/api/chat` SSE endpoint, with `streamChat` added to `site/lib/api.ts`. Reuses the backend's tool-use loop (search_listings, capture_lead) so it stays grounded in the listing book and records leads. Suggestions, typing dots, tool-status notes, graceful "API not running" fallback. Typechecks + builds clean. Runs over mock listings; will swap to Vault RE with no UI change. Live chat needs `ANTHROPIC_API_KEY` in `api/.env`. **All 5 client tools + concierge now built.**
- **2026-06-25 (tools complete)** — Built **Instant Valuation** (new site, wired to fixed `/api/valuation` via new `site/lib/api.ts`; suburb dropdown + comps + lead capture), **Portfolio assessment** (`lib/portfolio.ts` — value/debt/equity/LVR/usable-equity/yield), and **About + Contact** pages (Contact form → `/api/lead` with `source:"contact"`; extended the lead endpoint + `LeadInput` to accept contact source/intent). Verified the valuation reconcile fix corrects a swapped model output against real `sold.json`. **Milestone-2 calculators all done** (valuation, stamp-duty, pre-buying, portfolio). api + site typecheck + build clean. Remaining: Concierge page + Vault RE listings (blocked on API access) + listing-detail page.
- **2026-06-25 (later)** — Built **Pre-buying tool** (Milestone 2). Fixed the **valuation price/property mismatch** (owner feedback): the LLM was transposing comp prices onto wrong addresses → added `reconcileComps()` in `api/src/valuation.ts` that re-binds every cited comp's price/date to the ground-truth `sold` record by address (seed data confirmed clean). **Logo recolored for the black theme** (owner feedback: navy won't contrast): navy elements → white, gold kept → `site/public/manifest-logo-white.png` (+ `manifest-logo-mono.png`, cropped `manifest-mark.png`, `favicon.png`); wired the tower mark + wordmark into the nav. **Newsletter bot confirmed NOT part of the website** (it's the Milestone-3 trust engine, separate). Site typechecks + builds clean.
- **2026-06-25** — Built the **VIC Stamp-duty calculator** (Milestone 2). Deterministic engine (`site/src/lib/stampDuty.ts`) with the general/PPR/FHB schedules pulled live from sro.vic.gov.au; verified every band against SRO anchors ($600k general = $31,070, matches the audited Excel to the dollar). FHB **exemption + concession phase-in** built (the part the Excel left unfinished). Premium dark UI page with live result, breakdown, saving callout, rate-schedule reference + lead CTA. Typechecks clean. Next: wire remaining tool pages (Valuation, Pre-buying, Portfolio, Concierge) — currently stubs.

- **2026-06-24 (direction locked)** — Akshay's design direction set & written into all docs (CLAUDE.md, brief, this tracker, memory): **black theme, white fonts, gold accent, Raleway, motion hero (placeholder), inspiration-only/original, all tools**. Confirmed the `Fresh_Build/website` baseline is a demo copy → building the real site **net-new**. Properly accessed WHITEFOX + LUXE for direction (WHITEFOX = dark midnight-navy monochrome editorial; LUXE = serif/sans, heritage/trust/advisory tone) — for inspiration only, copies deleted. Next: build fresh dark design system + motion-hero Home (manifest-ui/frontend-design), then VIC stamp-duty (calc-verifier).
- **2026-06-24 (restart)** — Session restarted to load fixed fetcher. Crawled WHITEFOX + luxury-RE references → wrote `Assets/design-direction-brief.md`. North star: premium-agency calm + broker-grade tools + first-home-buyer warmth (keep navy/gold/Raleway, elevate via whitespace + photography-first cards + tools-as-hero panels). Next: apply elevation pass to `website/` Landing.
- **2026-06-24 (later)** — Cleaned Excel (#REF! names). Built 4 agents. Upgraded context-mode → v1.0.166 (restart pending). Scaffolded `Fresh_Build/website/` from demo_theme baseline — runs on :5175, renders, logo wired. Next: restart → design crawl → elevate look.
- **2026-06-24** — Pivoted from 2-tool demo to full overhaul. Created `Fresh_Build/`, asset guides, Akshay WhatsApp (domain + Vault). Logo + newsletter-bot blueprint received. Locked: custom React, Manifest-owned infra, $500/mo cap. Wrote Akshay's Anthropic-account setup steps ($200/mo cap). Owner approved building all 4 agents.
